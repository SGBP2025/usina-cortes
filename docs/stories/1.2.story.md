# Story EPIC-001.2: Criar Projeto Supabase e Migrations

## Metadata
- **Story ID:** EPIC-001.2
- **Epic:** EPIC-001 — Transformação SaaS
- **Sprint:** 1 — Foundation
- **Points:** 13
- **Status:** Approved
- **Priority:** P0 — Bloqueante para Auth e todas as features de dados
- **Depende de:** EPIC-001.1

## Story
**As a** desenvolvedor do projeto  
**I want** um banco de dados PostgreSQL no Supabase com schema completo e RLS configurado  
**So that** o sistema tenha persistência de dados multi-tenant segura desde o início

## Acceptance Criteria
- [ ] AC1: Projeto Supabase criado e variáveis de ambiente configuradas
- [ ] AC2: Migration `001_initial_schema.sql` criada com as 8 tabelas principais
- [ ] AC3: Row Level Security (RLS) habilitada em TODAS as tabelas com dados de usuário
- [ ] AC4: Políticas RLS garantem que usuário só acessa seus próprios dados (`auth.uid() = user_id`)
- [ ] AC5: Índices otimizados criados para queries críticas (user_id, status, created_at)
- [ ] AC6: Clients Supabase configurados: `lib/supabase/client.ts` (browser) e `lib/supabase/server.ts` (server)
- [ ] AC7: Migration executável via `supabase db push` sem erros
- [ ] AC8: Tipos TypeScript gerados via `supabase gen types typescript`

## Tasks / Subtasks

- [ ] T1: Setup do Supabase CLI e projeto
  - [ ] T1.1: `npm install @supabase/supabase-js @supabase/ssr`
  - [ ] T1.2: `npx supabase init` na raiz de `usina-saas/`
  - [ ] T1.3: Conectar ao projeto Supabase remoto via `supabase link`

- [ ] T2: Criar migration do schema inicial
  - [ ] T2.1: Criar `supabase/migrations/001_initial_schema.sql`
  - [ ] T2.2: Tabela `plans` (id, name, price_per_minute, included_minutes, concurrent_jobs, created_at)
  - [ ] T2.3: Tabela `user_credits` (id, user_id FK auth.users, plan_id FK plans, balance_minutes, total_purchased, total_consumed, updated_at)
  - [ ] T2.4: Tabela `processing_jobs` (id, user_id, status ENUM[pending/processing/completed/error], video_file_id, credits_consumed, started_at, completed_at, error_message, created_at)
  - [ ] T2.5: Tabela `video_files` (id, user_id, storage_path, original_name, size_bytes, duration_seconds, created_at)
  - [ ] T2.6: Tabela `generated_clips` (id, job_id FK processing_jobs, video_file_id, storage_path, start_time, end_time, duration, tiktok_description, instagram_description, youtube_title, created_at)
  - [ ] T2.7: Tabela `usage_metrics` (id, user_id, job_id, service ENUM[whisper/claude/ffmpeg], cost_usd, duration_seconds, created_at)
  - [ ] T2.8: Tabela `invoices` (id, user_id, amount_brl, stripe_payment_intent_id, status, created_at)
  - [ ] T2.9: Índices: `idx_jobs_user_id`, `idx_jobs_status`, `idx_clips_job_id`, `idx_credits_user_id`

- [ ] T3: Configurar Row Level Security (CRÍTICO — gate Quinn)
  - [ ] T3.1: Habilitar RLS em todas as tabelas: `ALTER TABLE x ENABLE ROW LEVEL SECURITY`
  - [ ] T3.2: Policy `user_credits`: SELECT/UPDATE WHERE `auth.uid() = user_id`
  - [ ] T3.3: Policy `processing_jobs`: SELECT/INSERT/UPDATE WHERE `auth.uid() = user_id`
  - [ ] T3.4: Policy `video_files`: SELECT/INSERT/DELETE WHERE `auth.uid() = user_id`
  - [ ] T3.5: Policy `generated_clips`: SELECT via JOIN com processing_jobs WHERE `auth.uid() = job.user_id`
  - [ ] T3.6: Policy `usage_metrics`: SELECT WHERE `auth.uid() = user_id`
  - [ ] T3.7: Policy `invoices`: SELECT WHERE `auth.uid() = user_id`

- [ ] T4: Configurar clients Supabase
  - [ ] T4.1: Criar `lib/supabase/client.ts` com `createBrowserClient()`
  - [ ] T4.2: Criar `lib/supabase/server.ts` com `createServerClient()` usando cookies
  - [ ] T4.3: Criar `lib/supabase/middleware.ts` para refresh de sessão

- [ ] T5: Seed de dados iniciais
  - [ ] T5.1: Criar `supabase/seed.sql` com INSERT do plano Starter: `id='starter'`, `name='Starter'`, `price_per_minute=0.10`, `included_minutes=30`, `concurrent_jobs=1`
  - [ ] T5.2: Criar plano Pro: `id='pro'`, `price_per_minute=0.08`, `included_minutes=100`, `concurrent_jobs=2`
  - [ ] T5.3: Criar plano Business: `id='business'`, `price_per_minute=0.05`, `included_minutes=500`, `concurrent_jobs=5`

- [ ] T6: Gerar tipos TypeScript
  - [ ] T5.1: `npx supabase gen types typescript --local > lib/supabase/types.ts`
  - [ ] T5.2: Verificar que tipos batem com o schema criado

- [ ] T7: Validação
  - [ ] T7.1: `supabase db push` executa sem erros
  - [ ] T7.2: Testar RLS: criar 2 usuários de teste, confirmar que usuário A não vê dados do usuário B
  - [ ] T7.3: `npm run build` sem erros de tipo TypeScript
  - [ ] T7.4: Confirmar que seed.sql insere os 3 planos corretamente

## Dev Notes

**Decisão Aria:** Usar **Supabase Auth nativo** — NÃO implementar JWT customizado como sugerido no DB-AUDIT.md original. O Supabase Auth já provê JWT, refresh tokens e integração direta com RLS via `auth.uid()`.

**CRÍTICO Quinn:** RLS deve estar ativa ANTES de qualquer dado de usuário ser inserido. A migration deve incluir as policies RLS no mesmo arquivo, não em migration separada.

**Status ENUM:** Usar `CREATE TYPE job_status AS ENUM ('pending', 'processing', 'completed', 'error', 'cancelled')`. Se Lovable/cliente precisar de valores adicionais, adicionar via migration, nunca modificar o tipo diretamente.

**Worker access:** O worker (rodando no Railway) usará `SUPABASE_SERVICE_ROLE_KEY` que bypassa RLS. Isso é intencional — o worker acessa dados de qualquer usuário para processar os jobs. Documentar isso explicitamente nos comentários do código.

**Supabase Storage:** Criar bucket `videos` (private) e `clips` (private) no Supabase Storage durante esta story. Configurar políticas de storage para acesso apenas do próprio usuário.

## Testing

- Teste de RLS: Criar usuario_a e usuario_b, inserir job para usuario_a, confirmar que query com auth.uid() de usuario_b retorna 0 rows
- Teste de migration: `supabase db reset` seguido de `supabase db push` — deve funcionar do zero
- Teste de tipos: Nenhum erro `any` no TypeScript para operações de DB

## QA Results
> A ser preenchido por @qa após implementação

## Dev Agent Record

### Agent Model Used
_[A ser preenchido pelo @dev]_

### Debug Log References
_[A ser preenchido pelo @dev]_

### Completion Notes List
_[A ser preenchido pelo @dev]_

### File List
_[A ser preenchido pelo @dev]_

### Change Log
_[A ser preenchido pelo @dev]_
